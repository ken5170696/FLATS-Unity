# Unity source synchronization — 23 September 2026

Reviewed the Unity project content at development snapshot `b58c0323256fb095595fb7cf0aba6207def62048` against this repository's baseline `824173f`. The comparison covered tracked `Assets`, `Packages` and `ProjectSettings`: 39 shared paths differed, 18 paths existed only in the development snapshot and 13 only in this standalone project.

## Integrated changes

- `FlatsWebObservation` now includes camera/render-target properties and RawImage surfaces in opt-in Web diagnostic snapshots. Normal observation enablement and sampling frequency are unchanged.
- `FlatsScopeVisualCheck` and its original asset GUID provide an additional rendered checkerboard fixture for all five sight prefabs. Output defaults to the ignored `Builds/ScopeVerification` directory; `FLATS_SCOPE_EVIDENCE` can select another directory.

The existing sight prefabs already contain the corrected UV serialization. Their additional shader references, navigation repairs (including Tutorial), scene teardown fixes, isolated profiles/leaderboard, runtime UI material ownership and standalone validation tools are retained. The shared MainUI material already matches the development snapshot.

Other differences are deliberate standalone settings: project-local report paths, build provenance, optional catalogue configuration, sanitized service configuration, package dependencies and platform settings. Historical migration/experimental utilities are not required by the playable project. Website deployment and distribution tools belong to their separate project. No development Git history was imported.

## Verification

Using Unity 6000.3.24f1 (`4e7b9b5b6244`):

- Repository file boundary and unique asset GUID check: PASS.
- `tools/unity.ps1 -Task Validate`: PASS, including scene/prefab references and module/storage regressions.
- `tools/unity.ps1 -Task Sights`: PASS for all five camera outputs and masked RawImage surfaces.
- `FlatsScopeVisualCheck.Capture`: PASS for all five checkerboards; generated images retained locally.
- `tools/unity.ps1 -Task Web`: PASS; WebGL/IL2CPP build completed with zero build errors. Browser gameplay was not re-tested in this synchronization.

Checks ran on baseline `824173f` plus the three source files integrated by this commit. The local build provenance records that baseline HEAD; the candidate build is not a published release.

To reproduce the additional fixture, close the project in the Editor and run:

```powershell
& '<Unity Editor>/Unity.exe' -batchmode -quit -projectPath "$PWD" -executeMethod FlatsScopeVisualCheck.Capture -logFile "$PWD/Logs/scope-grid.log"
```

Require exit code zero and five `SCOPE_GRID_PASS` entries. The existing commands and platform/runtime limitations remain in [VALIDATION.md](VALIDATION.md). This synchronization does not establish new multiplayer, mobile or full browser gameplay acceptance.
